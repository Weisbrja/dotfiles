#!/bin/sh

color=5

if setxkbmap -query | grep -Gq 'layout:\s*de'
then
	layout='De'
else
	layout='Us'
fi

printf ' ^c%s^Lay ^c%s^%s ' "$(get_xresource foreground:)" "$(get_xresource color$color:)" "$layout"
