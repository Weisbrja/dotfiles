#!/bin/sh

volume="$(pamixer --get-volume)"

if [ $(pamixer --get-mute) = false ]
then
	speakers='A'
else
	speakers='M'
fi

if amixer get Capture | grep -Fwq '[on]'
then
	microphone='R'
else
	microphone='M'
fi

printf " ^c#f1ebeb^Vol ^c#9358fe^%3s%% %s %s " "$volume" "$speakers" "$microphone"
