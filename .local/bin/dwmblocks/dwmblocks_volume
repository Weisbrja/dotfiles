#!/bin/sh

volume=$(pamixer --get-volume)

speakers=$(pamixer --get-mute | sed 's/false/A/;s/true/M/')

if amixer get Capture | grep -Fwq '[on]'
then
	microphone='R'
else
	microphone='M'
fi

printf ' ^c#f1ebeb^Vol ^c#dc2566^%3s%% %s %s ' "$volume" "$speakers" "$microphone"
