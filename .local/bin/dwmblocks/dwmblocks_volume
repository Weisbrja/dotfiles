#!/bin/sh

if [ $(pamixer --get-mute) = false ]
then
	volume="$(pamixer --get-volume)"
else
	volume='---'
fi

if amixer get Capture | grep -Fwq '[on]'
then
	microphone=' '
else
	microphone='M'
fi

printf " ^c#f1ebeb^VOL ^c#9358fe^%3s%% %s " "$volume" "$microphone"
