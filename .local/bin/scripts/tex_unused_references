#!/bin/sh

if [ ! "$@" ]
then
	echo "Usage: tex_unused_references file1 ..."
	exit 1
fi

for s in $(sed 's/%.*//' "$@" | grep '\\label{.*}' | sed 's/^.*\\label/ref/')
do
	grep -q "$s" main.tex || echo "$s" | sed 's/ref{//;s/}$//'
done
